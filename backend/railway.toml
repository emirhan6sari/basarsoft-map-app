# Railway servis kök dizini: backend (backend/BasarsoftOdev.Api DEĞİL)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# DB kontrolü /health/db — deploy sırasında migration bitmeden 500 dönebilir
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
