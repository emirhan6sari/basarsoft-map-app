[build]
# npm ci bazen node_modules/.vite EBUSY verir (Railway/Nixpacks önbelleği)
buildCommand = "rm -rf node_modules/.vite && npm install && npm run build"

[deploy]
startCommand = "npx --yes serve -s dist -l $PORT"
restartPolicyType = "ON_FAILURE"
